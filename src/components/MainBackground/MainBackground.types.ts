// Base dependencies
import React from "react";

/**
 *
 * MainBackground component props interface,
 *
 */
export interface MainBackgroundProps {
  /**
   * Main children node to be rendered
   */
  children?: React.ReactNode;
}
